# Generated by `phi theme set` (S-12) via design/adapters.txt in
# phios-dotfiles. Never edit by hand, never commit — see
# docs/tokens-example.md for a checked-in worked example (both files below).
/Config/Tokens.qml

# Interface rework: the colour half of the former Tokens.qml, split out so
# a variant switch no longer rewrites a singleton's own QML source (see
# Config/Colors.qml's header). Same generated/gitignored treatment.
/Config/Colors.json
